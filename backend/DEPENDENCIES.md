# Twitter Scraper Backend - 依赖对比

## 🏆 极简版 (Minimal) - 推荐

**文件:** `server_minimal.py`

### 依赖
```
kuzu==0.5.0
```

**仅 1 个依赖！** ✅

### 特点
- ✅ **最少依赖** - 只有 kuzu
- ✅ **标准库** - 使用 Python 内置模块
- ✅ **单文件** - 所有代码在 1 个文件中
- ✅ **快速** - 无框架开销

### 使用 Python 标准库
- `http.server` - HTTP 服务器
- `json` - JSON 处理
- `asyncio` - 异步支持
- `logging` - 日志
- `urllib.parse` - URL 解析

### 安装
```bash
pip install kuzu
```

### 启动
```bash
python server_minimal.py --port 8770
```

---

## Lite 版

**文件:** `server_lite.py`

### 依赖
```
kuzu==0.5.0
loguru==0.7.2
```

**2 个依赖**

### 特点
- 改进的日志输出
- 更好的错误处理

---

## Full 版

**文件:** `server.py`

### 依赖
```
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.0
kuzu==0.5.0
loguru==0.7.2
python-multipart==0.0.6
```

**6 个依赖**

### 特点
- FastAPI 完整功能
- Swagger UI 文档
- Pydantic 验证

---

## 对比表

| 版本 | 依赖数 | 代码行数 | 启动时间 | 内存 | 推荐场景 |
|------|--------|----------|----------|------|----------|
| **Minimal** | 1 | ~150 | <1 秒 | ~40MB | ✅ 本地开发 |
| **Lite** | 2 | ~200 | ~1 秒 | ~50MB | 日常使用 |
| **Full** | 6 | ~400 | ~3 秒 | ~100MB | 生产环境 |

---

## API 一致性

所有版本提供**完全相同**的 API：

- `GET /` - 服务信息
- `GET /health` - 健康检查
- `GET /api/stats` - 统计信息
- `GET /api/posts` - 获取帖子
- `GET /api/posts/filtered` - 筛选结果
- `GET /api/discovery/stats` - 发现性统计
- `POST /api/posts/batch` - 批量接收
- `POST /api/cleanup/run` - 清理任务

可以**无缝切换**版本！

---

## 依赖来源分析

### Minimal 版依赖树

```
kuzu==0.5.0
└── (无其他依赖)
```

**总计：1 个包**

### Python 标准库（无需安装）

```
http.server      # HTTP 服务器
json             # JSON 处理
asyncio          # 异步支持
logging          # 日志
urllib.parse     # URL 解析
pathlib          # 路径处理
argparse         # 命令行参数
datetime         # 日期时间
```

**总计：8 个内置模块**

---

## 推荐

### 选择 Minimal 版，如果：
- ✅ 想要最少依赖
- ✅ 本地开发测试
- ✅ 快速部署
- ✅ 资源受限环境

### 选择 Lite 版，如果：
- ✅ 需要更好的日志
- ✅ 可以接受 2 个依赖

### 选择 Full 版，如果：
- ✅ 需要 Swagger UI
- ✅ 需要自动重载
- ✅ 生产环境部署

---

## 当前状态

**✅ Minimal 版已运行**
- 地址：http://localhost:8770
- 依赖：仅 kuzu (1 个)
- 代码：~150 行
- 启动：<1 秒

---

## 快速切换

```bash
# Minimal 版 (推荐)
python server_minimal.py --port 8770

# Lite 版
python server_lite.py --port 8769

# Full 版
python server.py --port 8769 --reload
```

